{
  "name": "@trading-bot/strategies",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "example:grid": "npm run build && node dist/examples/dynamic-grid.example.js",
    "example:twap": "npm run build && node dist/examples/twap-vwap.example.js",
    "example:statarb": "npm run build && node dist/examples/stat-arb.example.js",
    "example:hotreload": "npm run build && node dist/examples/hot-reload.example.js"
  },
  "dependencies": {
    "@trading-bot/core": "*",
    "@trading-bot/risk-engine": "*",
    "chokidar": "^3.5.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
